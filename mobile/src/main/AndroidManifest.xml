<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet for HTTP / S3 uploads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground services: audio upload + phone recording watcher -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Post notifications (Android 13+) — required for persistent foreground notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Location for optional GPS location upload (network-based, low power) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Wake lock — keep service alive during upload -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Request battery optimization exemption — needed for keep-alive -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Receive boot completed — auto-start phone watcher on reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Read external storage — fallback when user enters path manually (Android 12 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Wearable Listener Service - receives audio from watch -->
        <service
            android:name=".service.AudioReceiverService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/audio"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/audio"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <!-- Persistent foreground service — keeps process alive so Samsung can't freeze it -->
        <service
            android:name=".service.WatchReceiverService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Audio Upload Service -->
        <service
            android:name=".service.AudioUploadService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Phone Recording Watcher Service — persistent foreground service -->
        <service
            android:name=".service.PhoneRecordingWatcherService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot Receiver — auto-start phone watcher on device boot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Declare wearable capability for watch discovery -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- FileProvider for sharing audio files to SpeechRecognizer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.omi4wos.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
